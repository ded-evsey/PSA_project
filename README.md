:In console:
1) git pull
2) install docker-compose
3) run "docker-compose run"
:In brouser:
4) go http://127.0.0.1:8000/dashboard (if not project, import from frontend dir)
5) tasks->serve->config->ports 5000-> save
6) run task
7) go http://localhost/ and click to btn
